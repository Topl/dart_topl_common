name: topl_common
description: Topl's Common and Proto definitions used to build the next blockchain for good.
repository: https://github.com/Topl/dart_topl_common

version: 2.1.0

environment:
  sdk: ">=2.19.5 <3.0.0"


dependencies:
  protobuf: ^3.1.0
  grpc: ^3.0.2
  fixnum: ^1.1.0
  fast_base58: ^0.2.1

dev_dependencies:
  test: ^1.22.1

platforms:
  android:
  ios:
  linux:
  macos:
  web:
  windows: